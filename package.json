{
  "name": "roommates",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run api\" \"npm run mobile\"",
    "api": "cd Roommates.API && npm run dev",
    "mobile": "cd Roommates.Mobile && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
